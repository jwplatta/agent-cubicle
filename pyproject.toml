[project]
name = "cubicle"
version = "0.1.0"
description = "Local harness for repo-managed AI coding agent configuration"
readme = "README.md"
requires-python = ">=3.9.2"
dependencies = [
  "ollama",
  "openrouter",
  "dotenv",
  "requests"
]

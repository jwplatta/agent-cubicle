FROM agent-cubicle-gemini-base:latest

USER root

RUN npm install -g @google/generative-ai

COPY bin/entry-gemini.sh /entry.sh
RUN chmod +x /entry.sh && chown gemini:gemini /entry.sh
RUN chown gemini:gemini /home/gemini

USER gemini

CMD ["/entry.sh"]
